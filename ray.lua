
---Ray Library -- Raycast and Collider system for Usagi
---
--- Usage:
---
--- --Import library
--- local Ray = require("ray")
--- 
--- -- use the modules
--- Ray.Collider          -- collider factory
--- Ray.ColliderRegistry  -- index of Collider instances
--- Ray.HitStrategy       -- factory of strategies to detect hits while performing a Raycast
--- Ray.Raycast           -- Raycast system
--- Ray.RaycastHit        -- object representing a hit found by Raycast

-- local colliders     = require("mod.colliders")
-- local Raycast       = require("mod.raycast")
-- local RaycastHit    = require("mod.raycasthit")
-- local HitStrategy   = require("mod.hitstrategy")
-- local PointBuffer   = require("mod.pointbuffer")

-- return {
--     Collider         = colliders.Collider,
--     ColliderRegistry = colliders.ColliderRegistry,
--     Raycast          = Raycast,
--     RaycastHit       = RaycastHit,
--     HitStrategy      = HitStrategy,
--     PointBuffer      = PointBuffer,
-- }
